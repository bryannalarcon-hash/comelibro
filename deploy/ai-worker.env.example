AI_SOCKET_PATH=/run/comelibro-ai/worker.sock
# Set this to the existing controlled owner's immutable application user ID.
# The same ID must be set in comelibro.env. Empty IDs fail closed.
AI_ALLOWED_USER_ID=
